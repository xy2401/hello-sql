# Neo4j 概览

Neo4j 是原生图数据库（Native Graph Database）的代表，专为高度关联的数据网络建模打造（如社交关系、风控反欺诈、知识图谱、推荐系统）。

## 核心优势

- **免索引邻接（Index-Free Adjacency）**：节点在磁盘和内存中直接持有指向相邻关系记录的物理指针，遍历图深度增加时查询耗时不会随全表数据量膨胀而暴增。
- **Cypher 图查询语言**：直观易读的声明式模式匹配语法（如 `(u:User)-[:FOLLOWS]->(f:User)`）。
