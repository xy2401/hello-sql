# TimescaleDB 概览

TimescaleDB 是以 PostgreSQL 插件形式存在的时序与事件数据库，将全功能关系型 SQL 与针对时序优化的自动分区、列式压缩完美融合。

## 核心功能

- **超表（Hypertable）**：对外展现为标准单个 SQL 表，底层按时间维度（及可选空间维度）自动切分为独立的 Chunk。
- **实时连续聚合（Continuous Aggregates）**：自动增量物化预计算聚合结果（如 5 分钟均值），大幅提升长周期报表查询速度。
- **原生列式压缩**：可按时间窗口将旧数据转为列式存储，节约高达 90% 以上磁盘空间。
