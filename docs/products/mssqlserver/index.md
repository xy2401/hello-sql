# SQL Server 概览

Microsoft SQL Server 是企业级关键业务应用中广泛采用的成熟商业关系数据库管理系统。

## 核心特性

- **Transact-SQL（T-SQL）**：强大的编程扩展方言。
- **存储与索引**：以 8KB 数据页（Data Page）和 64KB 区（Extent）为基本物理分配单元，支持聚簇索引、列存储索引（Columnstore Index）与内存 OLTP（In-Memory OLTP）。
- **高可用方案**：Always On 可用性组（Availability Groups），支持多副本自动故障转移与只读路由。
